[24bcs116@mepcolinux ex3]$cat QucikSort2.javac
import java.util.Arrays;
import java.util.Scanner;

public class QuickSort2 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter array size: ");
        int n = sc.nextInt();
        int[] arr = new int[n];
        System.out.println("Enter elements:");
        for (int i = 0; i < n; i++) arr[i] = sc.nextInt();

        System.out.println("\nStarting Quick Sort...");
        quickSort(arr, 0, n - 1);
        System.out.println("\nFinal Sorted Array: " + Arrays.toString(arr));
        sc.close();
    }

    static void quickSort(int[] arr, int low, int high) {
        if (low < high) {
            int pi = partition(arr, low, high);
            System.out.println("Pivot placed at index " + pi + ": " + Arrays.toString(arr));

            quickSort(arr, low, pi - 1);
            quickSort(arr, pi + 1, high);
        }
    }

    static int partition(int[] arr, int low, int high) {
        int pivot = arr[high];
        int i = (low - 1);
        for (int j = low; j < high; j++) {
            if (arr[j] <= pivot) {
                i++;
                int temp = arr[i];
                arr[i] = arr[j];
                arr[j] = temp;
            }
        }
        int temp = arr[i + 1];
        arr[i + 1] = arr[high];
        arr[high] = temp;
        return i + 1;
    }
}

[24bcs116@mepcolinux ex3]$java QuickSort2
Enter array size: 6
Enter elements:
5
7
3
4
8
2

Starting Quick Sort...
Pivot placed at index 0: [2, 7, 3, 4, 8, 5]
Pivot placed at index 3: [2, 3, 4, 5, 8, 7]
Pivot placed at index 2: [2, 3, 4, 5, 8, 7]
Pivot placed at index 4: [2, 3, 4, 5, 7, 8]

Final Sorted Array: [2, 3, 4, 5, 7, 8]
[24bcs116@mepcolinux ex3]$cat MergeSort3.java
import java.util.Arrays;
import java.util.Scanner;

public class MergeSort3 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter size: ");
        int n = sc.nextInt();
        int[] arr = new int[n];
        System.out.println("Enter elements:");
        for (int i = 0; i < n; i++) arr[i] = sc.nextInt();
        System.out.println("\n--- Starting Merge Sort ---");
        mergeSort(arr, 0, n - 1);
        System.out.println("\nFinal Result: " + Arrays.toString(arr));
        sc.close();
    }
    static void mergeSort(int[] arr, int left, int right) {
        if (left < right) {
            int mid = left + (right - left) / 2;
            System.out.println("Splitting: " + Arrays.toString(Arrays.copyOfRange(arr, left, right + 1)));
            mergeSort(arr, left, mid);
            mergeSort(arr, mid + 1, right);
            merge(arr, left, mid, right);
            System.out.print("  --> Merged: ");
            printRange(arr, left, right);
        }
    }
    static void merge(int[] arr, int left, int mid, int right) {
        int n1 = mid - left + 1;
        int n2 = right - mid;
        int[] L = new int[n1];
        int[] R = new int[n2];

        for (int i = 0; i < n1; ++i) L[i] = arr[left + i];
        for (int j = 0; j < n2; ++j) R[j] = arr[mid + 1 + j];

        int i = 0, j = 0, k = left;
        while (i < n1 && j < n2) {
            if (L[i] <= R[j]) arr[k++] = L[i++];
            else arr[k++] = R[j++];
        }
        while (i < n1) arr[k++] = L[i++];
        while (j < n2) arr[k++] = R[j++];
    }

    static void printRange(int[] arr, int low, int high) {
        int[] sub = Arrays.copyOfRange(arr, low, high + 1);
        System.out.println(Arrays.toString(sub));
    }
}

[24bcs116@mepcolinux ex3]$java MergeSort3
Enter size: 6
Enter elements:
4
6
2
3
8
1

--- Starting Merge Sort ---
Splitting: [4, 6, 2, 3, 8, 1]
Splitting: [4, 6, 2]
Splitting: [4, 6]
  --> Merged: [4, 6]
  --> Merged: [2, 4, 6]
Splitting: [3, 8, 1]
Splitting: [3, 8]
  --> Merged: [3, 8]
  --> Merged: [1, 3, 8]
  --> Merged: [1, 2, 3, 4, 6, 8]

Final Result: [1, 2, 3, 4, 6, 8]
